import './globals.css';

export const metadata = { title: 'Zenith Banking Web', description: 'Responsive Zenith-style banking assignment clone' };

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
